kind = "web"
previewPath = "/"
title = "Transport Management Platform"
version = "1.0.0"
id = "artifacts/transport-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20352

[services.development]
run = "pnpm --filter @workspace/transport-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/transport-app", "run", "build" ]
publicDir = "artifacts/transport-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20352"
BASE_PATH = "/"
